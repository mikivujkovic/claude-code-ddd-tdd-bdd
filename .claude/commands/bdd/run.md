---
description: Run BDD checks (if configured) and report results
allowed-tools: Bash, Read
---
Run `npm run -s test:bdd` if it exists. If not, print a short checklist to set it up.
Then re-run /verify.
