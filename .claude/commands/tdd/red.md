---
description: TDD step: write ONE failing test for a capability (RED)
argument-hint: [capability-name]
allowed-tools: Read, Edit
---
For "$ARGUMENTS":
- Identify the smallest domain rule from @docs/domain/invariants.md
- Add ONE test that fails for that rule (Given/When/Then style naming is fine)
- Explain why it is failing and what the minimal implementation should do next
Do not write UI tests.
